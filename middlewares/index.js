const checkIdentification = require("./checkIdentification");
const checkLuggage = require("./checkLuggage");
const checkRegistered = require("./checkRegisteredVisitor");

module.exports = { checkIdentification, checkLuggage, checkRegistered };
